{% block form_row %}

    <div class="error">
        {{ form_errors(form) }}
    </div>
    <div class="flex_div">
        {{ form_label(form) }}
        {{ form_widget(form) }}
    </div>

{% endblock %}
